/*
    C:\TEMP\TEST.TXT"　->　"TEST" を返す。
*/

$filename=getarg(0);
execmacro currentmacrodirectory + "\\load_dll.mac", "ht_tools.dll";
#dll=val(getresultex(-1));
if(#dll==0){
	endmacro "";
}
$basename = dllfuncstr(#dll,"GetFileBaseName", $filename);
freedll #dll;
endmacro $basename;

